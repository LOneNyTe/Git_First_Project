#include <stdio.h>

int main(){
    // int arr[5][5] first creates row and second column
    // row can also represented side by side in initializing
    // {1,2},{1,2} first curly bracket is 0 row and second is 1 row
}