#include <stdio.h>
#include <string.h>

int main(){
    char str1[20] = "hello";
    char str2[20] = "abc";
    strcat(str1,str2);
    puts(str2);
    puts(str1);

    // concatinates 2nd string to first string

}